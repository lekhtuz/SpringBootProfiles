-- DDL initialization script
-- Another option is schema-{platform}.sql

-- This is Spring initialization method. Hibernate method is completely different and they can not be used at the same time.

select sysdate from dual;
